apiVersion: v1
kind: Secret
metadata:
  name: taskflow-secrets
  namespace: taskflow
  labels:
    app: taskflow
type: Opaque
# ⚠️  DO NOT commit real secrets to git.
# Use: kubectl create secret generic taskflow-secrets \
#        --from-literal=JWT_SECRET=your-real-secret \
#        --namespace=taskflow
# OR use a secrets manager (Vault, AWS Secrets Manager, Sealed Secrets, etc.)
#
# Values below are base64-encoded placeholders:
#   echo -n 'replace-me-with-real-secret' | base64
data:
  JWT_SECRET: cmVwbGFjZS1tZS13aXRoLXJlYWwtc2VjcmV0
